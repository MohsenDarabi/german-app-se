import { describe, it, expect } from 'vitest';
// TODO[SRS-API-01]: Rote Tests (nur Skelett, keine echte Assertions)
describe('scheduler', () => {
  it('should plan next cards (TODO)', () => {
    expect(true).toBe(true);
  });
});
